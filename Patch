--- /dev/null
+++ src/interrupts.h
@@ -0,0 +1,18 @@
+#ifndef INTERRUPTS_H
+#define INTERRUPTS_H
+
+#include <stdint.h>
+
+/* CPU register state pushed by ISR/IRQ stubs */
+typedef struct regs {
+    uint32_t ds;
+    uint32_t edi, esi, ebp, esp, ebx, edx, ecx, eax;
+    uint32_t int_no, err;
+} regs_t;
+
+#endif /* INTERRUPTS_H */
--- src/isr.c
+++ src/isr.c
@@ -1,3 +1,4 @@
 #include <stdint.h>
+#include "interrupts.h"
 #include "util.h"
@@
-typedef struct regs {
-    uint32_t ds;
-    uint32_t edi, esi, ebp, esp, ebx, edx, ecx, eax;
-    uint32_t int_no, err;
-} regs_t;
--- src/irq.c
+++ src/irq.c
@@ -1,3 +1,4 @@
 #include <stdint.h>
+#include "interrupts.h"
 #include "util.h"
@@
-extern void keyboard_handler(struct regs *r);
+extern void keyboard_handler(regs_t *r);
@@
-struct regs {
-    uint32_t ds;
-    uint32_t edi, esi, ebp, esp, ebx, edx, ecx, eax;
-    uint32_t int_no, err;
-};
--- src/keyboard.c
+++ src/keyboard.c
@@ -1,2 +1,3 @@
 #include <stdint.h>
+#include "interrupts.h"
 #include "util.h"
@@
-void keyboard_handler(struct regs* regs)
+void keyboard_handler(regs_t* regs)
